diff --git a/src/Gui/StartupProcess.cpp b/src/Gui/StartupProcess.cpp
--- a/src/Gui/StartupProcess.cpp
+++ b/src/Gui/StartupProcess.cpp
@@ -431,10 +431,14 @@ void StartupPostProcess::setupLogo()
         }
         QPixmap px(path);
         if (!px.isNull()) {
+#if !defined(FREECAD_OHOS)
+            // Even with a QWidget parent, the OHOS QPA gives this status-bar
+            // logo a native QLabel window and launches a second Ability.
             auto logo = new QLabel();
             logo->setPixmap(px.scaledToHeight(32));
             mainWindow->statusBar()->addPermanentWidget(logo, 0);
             logo->setFrameShape(QFrame::NoFrame);
+#endif
         }
     }
 }
