diff --git a/src/Gui/Application.cpp b/src/Gui/Application.cpp
--- a/src/Gui/Application.cpp
+++ b/src/Gui/Application.cpp
@@ -2745,6 +2745,12 @@ void Application::setStyleSheet(const QString& qssFile, bool tiledBackground)
             // The color will then be set to the application's palette.
             // Limitation: it doesn't work if the .qss file on purpose sets the same color as
             // for normal text. In this case the default link color is used.
+#if defined(FREECAD_OHOS)
+            // Polishing these temporary labels creates a native QLabel window
+            // on the OHOS QPA, which is then mapped to a second Ability. Keep
+            // the application palette's link color instead.
+            (void)mw;
+#else
             {
                 QLabel l1, l2;
                 l2.setProperty("haslink", QByteArray("true"));
@@ -2760,6 +2766,7 @@ void Application::setStyleSheet(const QString& qssFile, bool tiledBackground)
                     qApp->setPalette(newPal);
                 }
             }
+#endif
         }
     }
     else {
