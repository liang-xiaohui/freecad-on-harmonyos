diff --git a/src/Base/Exception.h b/src/Base/Exception.h
--- a/src/Base/Exception.h
+++ b/src/Base/Exception.h
@@ -29,7 +29,10 @@
 #include <FCConfig.h>
 
 #include <csignal>
-#include <source_location>
+#if __has_include(<source_location>)
+# include <source_location>
+# define FC_HAS_SOURCE_LOCATION_HEADER 1
+#endif
 #include <string>
 
 #include "BaseClass.h"
@@ -38,7 +41,7 @@ using PyObject = struct _object;  // NOLINT
 
 // Remove once all used compilers support this
-#if defined(__cpp_lib_source_location)
+#if defined(FC_HAS_SOURCE_LOCATION_HEADER) && defined(__cpp_lib_source_location)
 # define HAVE_STD_SOURCE_LOCATION 1
 #else
 # undef HAVE_STD_SOURCE_LOCATION
 #endif
@@ -127,6 +130,7 @@ template<typename ExceptionType>
 
 //--------------------------------------------------------------------------------------------------
 
+#if defined(HAVE_STD_SOURCE_LOCATION)
 template<typename Exception>
 constexpr void THROWM_(
     const std::string& msg,
@@ -155,6 +159,7 @@ constexpr void FC_THROWM_(
 {
     THROWM_<Exception>(raw_msg, location);
 }
+#endif
 
 namespace Base
 {
