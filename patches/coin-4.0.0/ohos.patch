diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1111111..2222222 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -202,9 +202,15 @@ else()
     #include_directories("${OPENGL_INCLUDE_DIR}")
     if (NOT TARGET OpenGL::GL)
       list(APPEND COIN_TARGET_INCLUDE_DIRECTORIES ${OPENGL_INCLUDE_DIR})
-      list(APPEND COIN_TARGET_LINK_LIBRARIES ${OPENGL_gl_LIBRARY} ${OPENGL_glu_LIBRARY})
+      list(APPEND COIN_TARGET_LINK_LIBRARIES ${OPENGL_gl_LIBRARY})
+      if (OPENGL_glu_LIBRARY)
+        list(APPEND COIN_TARGET_LINK_LIBRARIES ${OPENGL_glu_LIBRARY})
+      endif()
     else()
-      list(APPEND COIN_TARGET_LINK_LIBRARIES OpenGL::GL OpenGL::GLU)
+      list(APPEND COIN_TARGET_LINK_LIBRARIES OpenGL::GL)
+      if (TARGET OpenGL::GLU)
+        list(APPEND COIN_TARGET_LINK_LIBRARIES OpenGL::GLU)
+      endif()
     endif()
   endif()
 endif()
@@ -577,4 +583,5 @@
-if(NOT (HAVE_WGL OR HAVE_AGL OR HAVE_CGL OR HAVE_GLX))
+if(NOT (OHOS OR CMAKE_SYSTEM_NAME STREQUAL "HarmonyOS") AND
+   NOT (HAVE_WGL OR HAVE_AGL OR HAVE_CGL OR HAVE_GLX))
   if (NOT TARGET OpenGL::GLX)
     if (OPENGL_glx_LIBRARY)
       list(APPEND COIN_TARGET_LINK_LIBRARIES ${OPENGL_glx_LIBRARY})
@@ -816,3 +823,5 @@ include(CMakePackageConfigHelpers)
 # ############################################################################
 # New CPACK section, please see the README file inside cpack.d directory.
-add_subdirectory(cpack.d)
+if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/cpack.d/CMakeLists.txt")
+  add_subdirectory(cpack.d)
+endif()
diff --git a/src/actions/SoCallbackAction.cpp b/src/actions/SoCallbackAction.cpp
--- a/src/actions/SoCallbackAction.cpp
+++ b/src/actions/SoCallbackAction.cpp
@@ -461,5 +461,9 @@ static void
 delete_list_elements(SbList<SoCallbackData *> & cl)
 {
   int n = cl.getLength();
-  for (int i = 0; i < n; i++) cl[i]->deleteAll();
+  for (int i = 0; i < n; i++) {
+    if (cl[i] != NULL) {
+      cl[i]->deleteAll();
+    }
+  }
 }
